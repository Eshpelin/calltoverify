<?xml version="1.0" encoding="utf-8"?>
<!-- Production traffic is HTTPS. These cleartext exceptions exist only so you can
     test against a Coordinator running on your own machine or an emulator host
     (10.0.2.2). They do not permit cleartext to any real domain. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
