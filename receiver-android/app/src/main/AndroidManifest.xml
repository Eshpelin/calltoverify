<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================= -->
    <!-- Permissions                                                   -->
    <!-- ============================================================= -->

    <!-- Talk to the developer's backend. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- SMS channel: receive inbound text messages. RESTRICTED on Google Play,
         which is why this app is sideload-only (see README). -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Missed-call channel: observe call state + read the caller number, and
         auto-reject so the user only pays for a free missed call. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <!-- Camera for scanning the pairing QR. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Persistent heartbeat service + its notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Restart the receiver service after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:name=".ReceiverApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallToVerify">

        <!-- Single-activity Compose UI. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inbound SMS receiver. High priority so we see the message promptly.
             Guarded by BROADCAST_SMS, which only the system holds. -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="2147483647">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Missed-call channel: observe ringing calls, read the caller number,
             auto-reject, and report the call. PHONE_STATE has no system-only
             guard permission, so it is plainly exported. -->
        <receiver
            android:name=".receiver.CallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Restart the heartbeat service on boot if the device was paired. -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Foreground service: registers, heartbeats, and drains the retry
             queue. dataSync type matches our "keep a connection alive" use. -->
        <service
            android:name=".service.ReceiverService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
