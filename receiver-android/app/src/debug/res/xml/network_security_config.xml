<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only. Permits cleartext to localhost / 127.0.0.1 / the emulator
     host (10.0.2.2) so you can test against a Coordinator on your own machine.
     This file overrides the release config (which denies all cleartext) for the
     debug variant only, so it never ships in a release APK. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
